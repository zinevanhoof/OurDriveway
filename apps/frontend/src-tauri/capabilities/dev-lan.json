{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dev-lan",
  "description": "Dev only: lets plugin-http reach Caddy on the dev machine's LAN address (VITE_API_BASE in .env.development). Enabled by tauri.dev.conf.json; tauri.conf.json lists only `default`, so a release build cannot talk to it.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://192.168.50.29:*" }]
    }
  ]
}
